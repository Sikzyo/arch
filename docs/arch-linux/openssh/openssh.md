# openssh

OpenSSH es un conjunto de herramientas que permite conectarse de forma segura a otros equipos usando el protocolo SSH, cifrando la comunicación.

1. Instalación

   Para instalar openssh, usa el siguiente comando:

   `sudo pacman -S openssh`
